// // Генерация событий. на примере сайта ламкова (лоадер при загрузки)
// const sectionElements = document.querySelectorAll("section");

// const animateSections = () => {
//   sectionElements.forEach((sectionElement) => {
//     sectionElement.classList.add("is-visible"); // проитерируемся по коллекции элем sectionEl. На каждой итерации у нас есть доступ к дом-элем конкретной секции sectionEl. добавляем этому элем кл is-visible
//   });
// };

// document.addEventListener("preloaderClose", () => {
//   animateSections();
// });
